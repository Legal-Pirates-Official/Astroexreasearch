<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="stylesheets/events.css" />
        <link rel="stylesheet" href="stylesheets/navbar.css" />
        <link
            rel="shortcut icon"
            href="images/favicon.ico"
            type="image/x-icon"
        />
        <link rel="stylesheet" href="stylesheets/footer.css" />
        <title>Events</title>
    </head>

    <style>
        .event-nav {
            background: #fa8c43;
            color: white !important;
        }
    </style>

    <body>
        <%- include("./partials/navbar.ejs") %>

        <!-- <section class="eventsContainer">
            <div class="straightLine"></div>
            <%# eventsArray.forEach(events => { %>
            <div class="eventsDiv">
                <span class="circleSpan"></span>
                <div class="leftDiv">
                    <p><%#= events.description_events %></p>
                </div>
                <div class="rightDiv">
                    <h1><%#= events.date_events %></h1>
                    <h2><%#= events.time_events %></h2>
                    <div class="imageDiv">
                        <img src="<%#= events.image_events %>" />
                    </div>
                    <h1><%#= events.name_events %></h1>
                </div>
            </div>
            <%# }) %>
        </section> -->

        <div>
            <h1 class="heading_events">Competitions</h1>
            <section>
                <div class="main-div">
                    <% eventsArray.forEach(events => { %> 
                    <% if (events.category_events == "competitions_events") { %>
                    <div class="all-div">
                        `
                        <div class="content-div">
                            <h1><%= events.date_events %></h1>
                            <h2><%= events.time_events %></h2>
                            <p><%= events.description_events %></p>
                            <div class="imageDiv">
                                <img src="<%= events.image_events %>" />
                            </div>
                        </div>
                        <div class="download-div">
                            <a href="<%= events.events_link %>"
                                >Click to continue</a
                            >
                        </div>
                    </div>
                    <% } %> <% }) %>
                </div>
            </section>
        </div>

        <%- include("./partials/footer.ejs") %>

        <script src="javascripts/navbar.js"></script>
    </body>
</html>
